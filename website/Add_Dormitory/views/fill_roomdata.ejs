<%- include("head.ejs") %>

<body>
    <div class="container">
        <%- include("sidebar.ejs") %>
        <!-- Main Content -->
        <main class="content">
            <!-- Card Section -->
            <section class="card">
                <h2>Add Dormitory</h2>
                <p>add dormitory facility</p>
                <div class="roomdata">
                    <h2>RoomData</h2>
                    <% data.forEach((item)=>{ %>
                        <div class="room">
                            <div class="row">
                                <span class="label">Room Name : <%=item.Room_Name %></span>
                                <span class="label">Room Type : <%=item.Room_Type %></span>
                            </div>
                            <div class="row">
                                <span class="label">Size : <%=item.Size %></span>
                                <span class="label">Monthly Rental (Contract 1 year) : <%=item.Monthly_Rental * 12 %></span>
                            </div>
                            <div class="row">
                                <span class="label">Room Status : <%=item.Room_Status  ? "true" : "false"%></span>
                                <span class="label">Number Available Room : <%=item.Number_Available_Room %></span>
                            </div>
                        </div>
                    <% }); %>      
                    <a href="/add_roomdata"><button>Add room</button></a>
                </div>
                <a href="/add_dormdata"><button type="submit" value="Submit" id="submit">Submit</button></a>
        </main>
    </div>

    <style>
        .roomdata {
            text-align:left
        }
        button {
            margin-top: 10px;
            padding: 5px 5px;
            font-size: 16px;
            width: 20%;
            max-width: 20%;
        }

        a {
            text-decoration: none;
        }

        .room {
            width: 80%;
            border: 2px solid black;
            padding: 20px;
            border-radius: 8px;
            margin: 20px;
            background-color: #e9e9ed;
        }

        .row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .label {
            font-weight: bold;
            color: #333;
        }
    </style>
</body>
</html>