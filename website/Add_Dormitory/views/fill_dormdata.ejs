<%- include("head.ejs") %>

<body>
    <div class="container">
        <%- include("sidebar.ejs") %>
        <!-- Main Content -->
        <main class="content">
            <!-- Card Section -->
            <section class="card">
                <h2>การเพิ่มหอพัก</h2>
                <p>กรอกข้อมูลของหอพัก</p>
                <form action="/insert_DormData" method="post" enctype="multipart/form-data">
                    <label>1. ชื่อของหอพัก</label>
                    <input type="text" name="Rental_Name" autocomplete="off" required placeholder="กรุณากรอกข้อความ">
                    <label>2. หอพักสำหรับผู้เช่าเพศไหน</label>
                    <select name="Gender" id="Type">
                        <option value="Man">ผู้ชาย</option>
                        <option value="Woman">ผู้หญิง</option>
                        <option value="Mix">ได้ทั้งผู้ชายและผู้หญิง</option>
                    </select>
                    <label>3. คำอธิบายของหอพัก</label>
                    <textarea name="Description" autocomplete="off" required placeholder="กรุณากรอกข้อความ"></textarea>
                    <label>4. ช่องทางติดต่อ</label>
                    <div class="contact">
                        <div>
                            <label>เบอร์โทรศัพท์</label>
                            <input type="text" name="Phone_number" autocomplete="off" required placeholder="กรุณากรอกข้อความ">
                        </div>
                        <div>
                            <label>ไอดีไลน์</label>
                            <input type="text" name="Line_ID" autocomplete="off" required placeholder="กรุณากรอกข้อความ">
                        </div>
                    </div>
                    <label>5. ที่ตั้งของหอพัก</label>
                    <input type="text" name="Location" autocomplete="off" required placeholder="กรุณากรอกข้อความ">
                    <label>6. เพิ่มรูปภาพหน้าปก (เพิ่มแค่ 1 รูปเท่านั้น)</label>
                    <input type="file" name="Photo" accept="image/*" required>
                    <label>7. เพิ่มรูปภาพหอพัก (ได้สูงสุด 50 รูป)</label>
                    <input type="file" name="Gallery" accept="image/*" required multiple>
                    <button type="submit" value="Submit" id="submit">ยืนยัน</button>
                </form>    
        </main>
    </div>

    <style> 
        form {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
            margin-left: 15%;
            text-align: left;
        }
        .contact {
            display: flex;
            flex-direction: row;
            margin-top: 10px;
            margin-left: 10%;
            width: 60%;
        }
        input {
            margin-top: 10px;
            margin-bottom: 10px;
            width: 80%;
            font-size: 16px;
        }
        textarea {
            margin-top: 10px;
            margin-bottom: 10px;
            width: 80%;
            height: 100px;
            padding: 12px 20px;
            box-sizing: border-box;
            border: 1px solid black;
            border-radius: 4px;
            resize: none;
            font-size: 16px;
        }

        select {
            margin-top: 10px;
            margin-bottom: 10px;
            width: 80%;
            padding: 5px 5px;
            font-size: 16px;
        }

        button {
            margin-top: 10px;
            padding: 5px 5px;
            font-size: 16px;
            width: 20%;
            max-width: 20%;
        }
    </style>
</body>
</html>