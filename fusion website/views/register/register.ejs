<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration</title>
    <%- include("../register/regis_style") %>
</head>
<body>

    <div class="container">
        <h1>ITRentHub</h1>
        <p>สมัครสมาชิก</p>

        <% if (message) { %>
            <p style="color: red; font-weight: bold;"><%= message %></p>
        <% } %>

        <form action="/register" method="POST">
            <label for="username">ชื่อผู้ใช้</label>
            <input type="text" id="username" name="username" value="<%= formdata?.username || '' %>" required>

            <label for="email">อีเมล</label>
            <input type="email" id="email" name="email" value="<%= formdata?.email || '' %>" required>

            <label for="phone">เบอร์โทรศัพท์</label>
            <input type="text" id="phone" name="phone" value="<%= formdata?.phone || '' %>" required>

            <label for="password">รหัสผ่าน</label>
            <input type="password" id="password" name="password" required>

            <label for="cpassword">ยืนยันรหัสผ่าน</label>
            <input type="password" id="cpassword" name="cpassword" required>

            <input type="hidden" name="roles" value="user"> <!-- Assigns role as 'user' -->

            <button type="submit" class="btn">สมัครสมาชิก</button>
        </form>
        <p class="bottom-text">มีบัญชีอยู่แล้ว? <a href="/login">เข้าสู่ระบบที่นี่</a></p>
    </div>
</body>
</html>
