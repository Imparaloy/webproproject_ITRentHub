<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <%- include("regis_style") %>
</head>
<body>

    <div class="container">
        <h1>Reset Password</h1>
        <h2 style="font-weight: 400; margin-top: 0px;">
            Account: <%= formdata?.login%>
        </h2>

        <% if (message) { %>
            <p style="color: red; font-weight: bold;"><%= message %></p>
        <% } %>

        <form action="/reset_password" method="POST">
            <label for="password">New Password</label>
            <input type="password" id="password" name="password" required>

            <label for="cpassword">Confirm Password</label>
            <input type="password" id="cpassword" name="cpassword" required>

            <input type="hidden" id="login" name="login" value="<%= formdata?.login%>">
            <input type="hidden" id="roles" name="roles" value="<%= formdata?.roles%>">

            <button type="submit" class="btn">Confirm</button>
        </form>
        <p class="bottom-text"><a href="/login<%= formdata?.roles == 'owner' ? '_owner' : '' %>">Back to Login</a></p>
    </div>
</body>
</html>
